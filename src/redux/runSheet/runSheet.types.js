import { createActionSet } from '../../helpers/Actions';

export const ADD_EVENT_DETAILS_SELECTED = createActionSet('ADD_EVENT_DETAILS_SELECTED');
export const ADD_ORDER_ID_SELECTED = createActionSet('ADD_ORDER_ID_SELECTED');
export const ADD_QUEUE = createActionSet('ADD_QUEUE');
